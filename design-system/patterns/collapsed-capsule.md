# Collapsed Capsule

## Composition

`Project accent bar -> Todo count -> urgent DDL dot`

## Rules

- Width stays fixed at `60px`.
- The urgent dot reflects only `danger` or `overdue` items.
- Click or keyboard activation expands back to the full panel.
