# Changelog

## 0.1.0 - 2026-04-29

- Added typed token source in `src/design-system/tokens.ts`.
- Added runtime CSS variables and dark theme overrides in `src/styles/tokens.css`.
- Added P0 component set, key patterns, Ladle stories, and baseline keyboard tests.
- Added stylelint token-usage guardrails.

## Migration Notes

- Initial release. No migration required.
